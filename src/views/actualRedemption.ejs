<% include ./partials/header %>

<div class="container mt-5">
  <div class="row">
    <div class="col">
      <h1>Ο χρήστης του MyFuel <%= user.firstName %> <%= user.lastName %> θα έχει έκπτωση <%= redeemPoints/40 %>&euro; από την τρέχουσα αγορά του.</h1>
    </div>
  </div>
  <div class="row mt-4 text-center">
    <div class="col">
      <form action="/users/<%= user.id %>/redeem/<%= token %>/?redeemPoints=<%= redeemPoints %>" method="post">
        <button class="btn btn-primary btn-lg">
          <i class="fas fa-check"></i>
            Ολοκλήρωση συναλλαγής</button>
      </form>
    </div>
  </div>
</div>



<% include ./partials/footer %>
